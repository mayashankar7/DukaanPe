eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

spring:
  datasource:
    url: jdbc:h2:mem:synctestdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

